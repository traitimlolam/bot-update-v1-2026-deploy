FB_PAGE_ACCESS_TOKEN=
FB_APP_SECRET=
FB_VERIFY_TOKEN=
# Không bắt buộc — nếu để trống, reminderService tự tra cứu qua Graph API (GET /me?fields=id)
# bằng FB_PAGE_ACCESS_TOKEN ở trên. Chỉ cần điền tay nếu muốn ghi đè/tránh gọi thêm 1 lần API.
FB_PAGE_ID=
GOOGLE_SERVICE_ACCOUNT_JSON_PATH=
GOOGLE_SHEET_ID=
FIRESTORE_PROJECT_ID=
PORT=3000
# API key lấy từ Google AI Studio (Gemini Developer API, free tier) — dùng cho lớp trả lời tự do
# bằng AI (mục 4.2 CLAUDE.md). KHÔNG phải Vertex AI, KHÔNG liên quan gói thuê bao Google AI Pro/Ultra.
GEMINI_API_KEY=
